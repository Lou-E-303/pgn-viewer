import Ctrl from './ctrl';
import { Config as CgConfig } from 'chessground/config';
import { VNode } from 'snabbdom';
export default function view(ctrl: Ctrl): VNode;
export declare const makeConfig: (ctrl: Ctrl) => CgConfig;
